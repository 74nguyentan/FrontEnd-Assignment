'use strict';

describe('myApp.viewTraining module', function() {

  beforeEach(module('myApp.viewTraining'));

  describe('viewTraining controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewTrainingCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});