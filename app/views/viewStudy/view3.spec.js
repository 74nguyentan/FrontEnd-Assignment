'use strict';

describe('myApp.viewStudy module', function() {

  beforeEach(module('myApp.viewStudy'));

  describe('viewStudy controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewStudyCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});