'use strict';

describe('myApp.viewQuiz module', function() {

  beforeEach(module('myApp.viewQuiz'));

  describe('viewQuiz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewQuizCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});