'use strict';

describe('myApp.viewGioiThieu module', function() {

  beforeEach(module('myApp.viewGioiThieu'));

  describe('viewGioiThieu controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ViewGioiThieuCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});